import { INetworkTypes } from "../types/types.ts";

export const NETWORK_TYPES: INetworkTypes = Object.freeze({
  ETHEREUM: "ETHEREUM",
  BINANCE: "BINANCE",
  POLYGON: "POLYGON",
});

export const MAX_TIME_FRAME = 90;
export const MID_TIME_FRAME = 30;
export const MIN_TIME_FRAME = 7;

export const MAX_TIME = 14;
export const MIN_TIME = 0;

export const SECOND_IN_MILLISECONDS = 1000;

export const DIVIDE_MAX_TIME_FRAME = 9;
export const DIVIDE_MID_TIME_FRAME = 5;
